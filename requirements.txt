# Dependencias runtime de MeteoLabX.
#
# Aquí vivía el stack visual de la interfaz anterior —streamlit, plotly,
# pydeck y streamlit-autorefresh—, fijado con ``==`` porque su CSS dependía de
# internals del DOM de Streamlit. Esa interfaz se ha retirado: la web la sirve
# el servicio SvelteKit y este backend es FastAPI puro. Un test
# (``tests/backend/test_sin_streamlit.py``) comprueba que no vuelve a colarse.
#
# Notas:
#   - pytest es dependencia de tests; idealmente debería vivir en un
#     requirements-dev.txt aparte. Se deja aquí por compatibilidad con la
#     configuración actual de CI.

requests>=2.31.0,<3.0.0
pandas>=2.0.0,<3.0.0
numpy>=1.26,<3.0
# SHARPpy se instala con `--no-deps` en railway.toml: su wheel 1.4.0a5
# funciona con NumPy actual, pero declara por error un pin obsoleto a 1.15.
rasterio>=1.4,<2.0
python-dotenv>=1.0,<2.0
Pillow>=10.0,<13.0
astral>=3.2,<4.0
# Nombres de país localizados (ES/EN/FR) para el selector del Ranking. Venía
# transitivo con Streamlit y pydeck; retirados esos, es dependencia directa.
Babel>=2.12,<3.0
pytest>=8.0.0,<9.0.0
# Sólo para tests: valida la helicidad relativa a la tormenta contra una
# implementación de referencia. Los tests que la usan se saltan si falta.
metpy>=1.6.0,<2.0.0

# --- Backend FastAPI (paquete `server/`) ---
# El backend convive con la app Streamlit durante la migración. Estas
# dependencias solo se usan si se arranca ``uvicorn server.main:app``;
# nada en Streamlit las importa hoy, así que añadirlas no cambia el
# comportamiento del frontend en producción. Cuando se complete la
# migración, esto se podrá reorganizar en requirements-backend.txt aparte.
fastapi>=0.110.0,<1.0.0
# Point-in-polygon (fronteras de países) para asignar país por coordenadas a
# las estaciones IEM/WMO sin código de país en el ranking. Ver
# data/ne_50m_admin_0_countries.geojson.
shapely>=2.0,<3.0
uvicorn[standard]>=0.27.0,<1.0.0
pydantic-settings>=2.2.0,<3.0.0
httpx>=0.27.0,<1.0.0
pytest-asyncio>=0.23.0,<1.0.0
# Cliente S3 para compartir frames precalculados entre el worker y la API
# mediante Railway Storage Buckets.
boto3>=1.35,<2.0
