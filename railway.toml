[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python scripts/patch_streamlit_index.py && streamlit run meteolabx.py --server.port=8501 --server.address=0.0.0.0 --server.headless=true"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
