{
  "name": "meteolabx-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run maplibre:worker && vite dev --host 0.0.0.0",
    "build": "npm run maplibre:worker && vite build",
    "preview": "node server.js",
    "start": "node server.js",
    "test": "node --import ./tests/alias.mjs --test tests/*.test.mjs",
    "maplibre:worker": "node scripts/copy-maplibre-worker.mjs"
  },
  "dependencies": {
    "@lucide/svelte": "^1.25.0",
    "http-proxy": "^1.18.1",
    "maplibre-gl": "^6.7.0"
  },
  "devDependencies": {
    "@sveltejs/adapter-node": "^5.2.12",
    "@sveltejs/kit": "^2.20.0",
    "@sveltejs/vite-plugin-svelte": "^6.0.0",
    "svelte": "^5.0.0",
    "vite": "^7.0.0"
  }
}
