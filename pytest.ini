[pytest]
# Configuración de pytest para MeteoLabX.
#
# - asyncio_mode = strict: cada test async debe marcarse explícitamente con
#   @pytest.mark.asyncio. Evita conflictos con tests sync legacy.
# - Sin testpaths: pytest descubre tests bajo `tests/` (default).
asyncio_mode = strict
